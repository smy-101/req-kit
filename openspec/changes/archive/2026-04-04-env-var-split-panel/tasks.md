## 1. 后端优化

- [x] 1.1 优化 `getAllEnvironments` 查询：将 N+1 模式改为两次 SQL（一次查环境，一次查所有变量），在 JS 层按 `environment_id` 分组

## 2. 前端布局重构

- [x] 2.1 重写 `env-manager.js` 的 `showEnvModal` 函数，生成分栏布局 HTML（左侧 `.env-panel-left` + 右侧 `.env-panel-right`）
- [x] 2.2 实现左侧环境列表渲染：环境名可点击切换，当前选中项高亮，底部新建输入区
- [x] 2.3 实现右侧变量编辑器：选中环境后显示 KV 行编辑器，未选中时显示提示文字

## 3. 交互逻辑

- [x] 3.1 实现点击环境名切换逻辑：选中 → 右侧渲染变量编辑器
- [x] 3.2 实现 `dirty` 标志：变量 input/change 事件触发设置 `dirty = true`，保存成功后重置为 `false`
- [x] 3.3 实现切换环境时的未保存修改确认：弹出三选一对话框（保存/丢弃/取消）
- [x] 3.4 实现环境重命名：Modal 内触发，调用 `api.updateEnvironment`，成功后刷新列表和编辑器标题
- [x] 3.5 实现环境删除：确认后删除，若删除的是当前选中环境则右侧恢复默认提示
- [x] 3.6 实现新建环境：创建后刷新列表

## 4. 变量编辑器优化

- [x] 4.1 保存变量后不重建 Modal，仅刷新变量数据
- [x] 4.2 实现重复 Key 检测：渲染时扫描变量数组，重复 Key 行添加警告样式

## 5. 样式

- [x] 5.1 添加分栏布局 CSS（`.env-panel-left`、`.env-panel-right`、Flexbox、最小宽度 600px）
- [x] 5.2 添加环境列表项样式（默认态、hover 态、选中高亮态）
- [x] 5.3 添加重复 Key 警告样式（红色边框或背景）
- [x] 5.4 添加重命名操作的 UI 样式

## 6. 测试

- [x] 6.1 更新或新增 `getAllEnvironments` 相关单元测试，验证变量正确分组
- [x] 6.2 手动验证完整编辑流程：新建环境 → 编辑变量 → 切换环境 → 未保存确认 → 重命名 → 删除
